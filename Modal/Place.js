export default class Place {
  constructor(id, title, imagePath, address, coordinates) {
    this.id = id;
    this.title = title;
    this.imagePath = imagePath;
    this.address = address;
    this.coordinates = coordinates;
  }
}
